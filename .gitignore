# HUNL turn datagen pilot data are committed ONLY upon full gate PASS
# (in-progress generation; verdict pending). Remove this entry at gate commit.
certification/hunl_datagen_pilot/

# build artifacts / caches (sources are committed; binaries are rebuilt)
__pycache__/
*.o
*.a
*.pyc
certification/scripts/CFR_plus_patched/cfr_patched
certification/scripts/CFR_plus_patched/compressor/compressor_test
.pytest_cache/
