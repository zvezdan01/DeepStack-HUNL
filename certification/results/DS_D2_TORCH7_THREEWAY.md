# D2 RESOLUTION — VARIANT B: RELEASED-LUA (TORCH7) THREE-WAY EXPERIMENT

Date: 2026-08-12. Status: **COMPLETE — D2 PROVEN as author-side provenance
artifact. Verdict: GO for bulk DataGeneration.**

Question under test: the 134/200 bundled TrainSamples target rows that our
certified Python engine does not reproduce byte-exactly (D2 in
`DS_PHASE2A_CERTIFICATION.md`) — are they (a) an artifact of the bundled
samples' provenance (generated by a pre-release author solver), or (b) a bug
in our engine relative to the released Lua reference?

Method: rebuild an authentic Torch7 runtime, run the **released, unmodified
Lua solver** on the exact 200 reconstructed inputs, and three-way compare
(bundled historical vs Lua fresh vs Python fresh) with zero tolerance on raw
IEEE-754 bytes.

Constraint honored: Golden Baseline `2ab6dde943ebad2f5458779dc561ed203f796e39`
and Phase-2 generator `7967622e5b66e35a8e9a17506f97db86c2e6b543` were not
modified (verified: both branches at those SHAs, DS working tree clean).

## 1. Torch7 runtime rebuild (succeeded)

Built from authentic upstream sources in an isolated prefix:

- LuaJIT 2.1 + `paths` + `cwrap` + `torch7` (torch/torch7 @ `814ea4a`,
  the same source tree already used for the THRandom C oracle; provenance
  previously anchored).
- Compile flags: `-O2 -ffp-contract=off` (no FMA contraction — matches the
  numeric contract certified for the Python side).
- BLAS: linked against the repo-bundled
  `libopenblas_sandybridgep-r0.3.0.dev.so`
  (SHA-256 `cd143947…`, the exact library the Python engine loads as
  `torch7_openblas.so.0`), via `BLAS_LIBRARIES` + a SONAME symlink
  `libopenblas.so.0`. Both runtimes therefore share one BLAS binary.
- One documented, numerically neutral compatibility stub: `package.preload['nn']`
  returns a table that **errors on any access**. `Nn/value_nn.lua` requires
  `nn` at module load, but `ValueNn` is never constructed for street-2
  resolves; the trap proves the stub was never touched (no error occurred in
  any run).
- No Lua source file of the released solver was modified.

### Runtime validation (gate before the experiment)

The rebuilt runtime regenerated the author-side one-iteration trace
(`reference_lua` exporter): **232/232 files byte-identical** to the frozen
`lua_trace/` corpus, and `torch.manualSeed(42)` reproduces the certified
MT19937 stream. The rebuilt stack is therefore bit-exact with the runtime
that produced the frozen oracle traces.

## 2. Experiment

Inputs: the 200 TrainSamples rows inverted by the certified Phase-2A
inversion (boards from mask blocks — exported 1-based for Lua; pot from
`pot_feature × 1200` f32; card ranges from the losslessly re-bucketized
input blocks). Exported as `d2_boards.t7`, `d2_features.t7`,
`d2_ranges.t7` (SHA-256 below).

Harness (`certification/d2_torch7/d2_harness.lua`) replicates
`Source/DataGeneration/data_generation.lua:98–112` exactly, per row: fresh
`Resolving()`; `current_node.board = arguments.Tensor{board}`; street 2;
current player P1; `pot_size = pot_feature * arguments.stack`;
`bets = {pot_size, pot_size}`; `resolve_first_node`;
`get_root_cfv_both_players()`; `mul(1/pot_size)`. Full original
1000-iteration / skip-500 resolve per row. Output: raw float32 card-space
CFVs `(200, 2, 6)`, saved **before** any formatting.

Python fresh recompute: same 200 inputs through the Golden Baseline engine
(with the certified `LuaF32TreeBuilder` injection), same protocol.

Comparator (`certification/d2_torch7/d2_threeway.py`): per row, bucketize
Lua and Python card CFVs with the certified `BucketConversionF32` and
compare all three sources with `np.array_equal` (bit-level). Additionally,
raw card-space Lua-vs-Python bytes compared directly (pre-bucketization).

## 3. Results

### Raw card-space, released Lua vs Python (the engine-equivalence claim)

**All 2400 float32 values byte-identical (200/200 rows).**
`lua.tobytes() == py.tobytes()` → True. Class: **BIT_EXACT**.

### Three-way classification, 200 rows (bucket space, vs historical targets)

| Class | Meaning | Count |
|---|---|---|
| A | Lua == Py == Historical | **66** |
| B | Lua == Py ≠ Historical | **134** |
| C | Lua == Historical ≠ Py (our engine bug) | **0** |
| D | all three differ | **0** |
| anomaly | Py == Historical ≠ Lua | **0** |

The B set is exactly the 134-row divergence set from the Phase-2A census;
the A set is exactly the 66 previously byte-exact rows.

### Frozen case (valid row 1, target idx 20 — P1, card Ks)

| Source | f32 bits | value |
|---|---|---|
| Historical (bundled `valid.targets`) | `0x40B4F2B3` | 5.654626369476318 |
| **Released Lua, fresh** | `0x40B4F2BC` | 5.654630661010742 |
| Python Golden Baseline, fresh | `0x40B4F2BC` | 5.654630661010742 |

The released Lua reproduces our value **bit-for-bit** (9 ULP away from the
bundled historical value). (Note: earlier Phase-2A notes transcribed these
bit patterns with a typo as `0x40B4F323`/`0x40B4F32C`; the float values
were and are correct — the patterns above are read directly from the
buffers.)

## 4. Conclusion

**D2: PROVEN** to be an author-side provenance artifact of the bundled
TrainSamples. The released Lua solver itself does not reproduce the bundled
targets on the same 134 rows — and it agrees with our Python engine to the
bit on all 200 × 2 × 6 raw values. The bundled samples were generated by a
different (pre-release) solver build, consistent with the independent
evidence already recorded (pre-release `final_cpu.info` schema, import-only
git history, per-row conditional split).

Consequences:
- The bundled TrainSamples remain a **mixed oracle**: inputs/masks BIT_EXACT,
  targets NUMERIC (≤5e-3) — now with a proven explanation.
- The "no bulk generation while an unexplained divergence exists" rule is
  satisfied: no unexplained divergence remains. **GO for bulk
  DataGeneration** with the certified generator (Phase-2A matrix +
  this experiment).
- Variant A rider is not needed.

## 5. Artifact hashes (SHA-256)

```
d2_boards.t7        9c19ee4d4c6be07ea5498820574d8e6581a31ab50fec0ae70495f92c04d92de6
d2_features.t7      b5793e53d6e1b837ffd1c20efa78138efcac6b291a2d053212d2da438c7c88a5
d2_ranges.t7        b44ea1dddff7f2b24dd279487400f7d8280ee3a56f8dcd2de1b1fca04d0dd509
d2_lua_fresh.t7     d1ed027e9d91369d8a74a5d4d41497e3a9fc1835a7cf9f41fb89686d2ac6fcc9
d2_python_fresh.npy de7347df7a0c6d6c0fdbc4851d7ed801b868d354a762cd25477827db71824f21
d2_harness.lua      6c5bf0f5991d76b8093c1653f0932202f51776fa160aaa894eaa291ffb5aca6f
d2_threeway.py      5c1902aff1397706931d976f737026ce8446ab3c98902b62f92e2d387d79b1c8
```
