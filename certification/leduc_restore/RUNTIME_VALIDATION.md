# Torch7 runtime restoration — build + validation record

Date: 2026-08-14 (session claude/generator-dat-pro-huhl-forenzic-rvclmx).
Purpose: step 1 of the golden-Leduc-engine restoration (port deferred
until the HUNL datagen pilot + certification complete).

## Sources (all pinned/verified)
- LuaJIT 2.1 branch (github LuaJIT/LuaJIT @ 1edc3e5, v2.1)
- torch/paths @ 4ebe222, torch/cwrap @ dbd0a62, torch/nn @ 8726825 (cloned,
  nn not yet built — the D2 method stubs `nn` for street-2 resolves)
- torch/torch7 @ 814ea4a — the RECORDED provenance commit (same tree as
  the THRandom C oracle; lib/TH/THRandom.c SHA 57524962…)
- lifrordi/DeepStack-Leduc @ da416f96 — the RECORDED upstream anchor;
  weights verify byte-exact vs recorded SHAs (final_cpu.model d5fcba44…,
  final_cpu.info 9ff711ff…)

## Build
`build_torch7_runtime.sh`: isolated prefix /workspace/torch_runtime,
flags `-O2 -ffp-contract=off` (the DS_D2 numeric contract). cmake notes:
torch ecosystem needs luarocks-style LUADIR/LIBDIR, and torch7's
TorchPaths.cmake requires them ABSOLUTE (computes RELATIVE_PATH vs the
install prefix).

## Validation (RNG bit-continuity with the certified stream)
After `torch.manualSeed(42)` the runtime produces:
- `torch.random(0, 4294967295)` -> **1608637542** == first u32 of the
  frozen certified seed-42 stream (`certification/oracles/rng/
  mt_seed42_n10000.txt` line 1; stream SHA d85832ea…, the Phase-2A
  cross-anchor).
- `FloatTensor:uniform(0,1)` (fresh manualSeed(42)) -> 0.37454012,
  0.79654300, 0.95071429 == f32(u32_k * 2^-32) for stream values
  1608637542, 3421126067, 4083286876 — the exact Torch7 __uniform__
  semantics certified in datagen/th_random.py.

## Recorded environment delta vs the lost pre-loss runtime
The DS-bundled OpenBLAS (`cd143947…`) and the frozen `lua_trace/` corpus
died with the DS workspace. This runtime links the BLAS resolved by
torch7's cmake in THIS container. Consequence: the restored engine will
be certified against FRESH oracles generated by this authentic runtime
(new fully-provenanced baseline), not against the lost corpus; any
comparison to surviving pre-loss numeric anchors is reported as
environment-dependent (FAILURE_MATRIX F-07 discipline).
